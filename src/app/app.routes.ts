import { Routes } from '@angular/router';
import { ChartComponent } from './weather/chart/chart.component';

export const routes: Routes = [
  {
    path: 'view',
    component: ChartComponent,
  },
];
